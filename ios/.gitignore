# Xcode — the .xcodeproj is regenerated from project.yml by xcodegen, so
# we treat it as a build artifact instead of source.
*.xcodeproj/
*.xcworkspace/
build/
DerivedData/
*.xcuserstate
*.moved-aside

# Swift Package Manager
.swiftpm/
Packages/
.build/

# CocoaPods (if you add them later)
Pods/

# fastlane
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots
fastlane/test_output

.DS_Store
