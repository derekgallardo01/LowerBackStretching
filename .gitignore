.claude/
.DS_Store

# Generated by scripts/inject_animations.py for visual QA.
scripts/preview.html

# Release signing — never commit the keystore or its credentials.
keystore.properties
*.jks
*.keystore

# Local IDE / build artifacts.
.idea/
.gradle/
android/.gradle/
android/.idea/
android/local.properties
android/app/build/
android/core/build/
android/wear/build/
release/

# Google Cloud service account keys � never commit credentials.
*-*.json
!firebase.json
!firestore.rules
!firestore.indexes.json

# Firebase client configs — restore from the console / CI secrets, never commit.
GoogleService-Info.plist
google-services.json
